get.test
创建和查看用户信息：
git config --global user.name "liuli"
git config --global user.email "liuli.lc01@inspur.com"
--global参数表示应用到本机所有库。

创建存储库：
新建目录：mkdir <git库目录名>
进入 新建目录，运行
git init
git add  <filename>
git commit -m <message>
github
